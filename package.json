{
  "name": "career-ops-local",
  "version": "1.0.0",
  "description": "Career-Ops local server — Playwright + PDF + Anthropic API proxy",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.44.0",
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
